# cache files for sublime text
*.tmlanguage.cache
*.tmPreferences.cache
*.stTheme.cache

# workspace files are user-specific
*.sublime-workspace

# project files should be checked into the repository, unless a significant
# proportion of contributors will probably not be using SublimeText
# *.sublime-project

# sftp configuration file
sftp-config.json

#project-specific ignores
pyenv*
.DS_Store
*.zip
build/*
!build/.gitkeep
build.pyc
#local capture client logs (logging/ is a log directory, not a python package)
logging/
config/
!config/
config/*
!config/facecompare-params.example.json
!config/faceverifier-params.example.json
#web-ui/logs/*
#web-ui/node_modules/
#web-ui/bower_components/
#web-ui/tmp
lambda/imageprocessor/pytz*
.venv/

# web-ui FastAPI backend: local secrets + venv + caches (never commit/upload)
web-ui/backend/.env
web-ui/backend/.venv/
web-ui/backend/data/
**/__pycache__/
*.pyc

#IDE files
.idea/
