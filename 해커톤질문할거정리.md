결국 해커톤 대상이 목표이므로, 심사위원의 관점에 대해 물어보는 것이 중요할듯.

설진우

1. 심사함에 있어서 주로 어느 측면을 중점적으로 보시냐. 구현성이라든지, 아니면 기대효과라든지, 실현가능성이나 참신성이라든지.
2. 서울시 지하철 보관함에 Smart AI Security라는 SW를 탑재하는 건데, 비용적 절감을 위해 API를 활용하긴 했으나, 실제 환경에서는 어느 정도로 예산을 편성받을 수 있을지에 대한 보강 방법 (예산 시스템이나, 서울시에 할당되는 예산)
3. Smart AI Security 시스템은 사실 결론적으로는 CV, VLM, RAG라서 이름만 다를 뿐 비슷하게 느껴질 수 있는 요소라고 생각하고 또 비슷한 팀이 냈을 수도 있다는 생각이 있는데, 혹시 심사위원의 관점에서 설함 팀의 어느 요소가 어필이 됐고 또 차별화 된 것으로 보였는지. (그를 바탕으로 논리 보강 예정)
4. 요소 중에 Culture CPTED라는 부분이 있는데, 이에 대한 견해가 어떠신지 궁금하고 유동인구의 증가 및 시민의 문화적 참여와 관람으로 인한 지속적 참여가 범죄율 감소에 기여할 것이라는 것에 동의하시는지가 궁금합니다. 

-------------

함승진

1. 개인정보보호법 때문에 CCTV 영상 활용이 까다롭고, 실제 기관이나 직원들도 데이터 활용에 부담을 느낀다고 말씀해주셨는데, 구체적으로 어떤 규정이나 상황 때문에 영상 활용이 어려운지 궁금합니다.
또한 실제 현장에서는 영상 수집, 저장, 열람, 분석, 외부 제공 중 어떤 부분을 가장 부담스러워하는지 궁금합니다.

2.  현재 물품보관함에 소형 카메라가 탑재되어 있는 것으로 알고 있습니다.
해당 카메라의 영상을 서비스에서 활용하거나 분석할 경우, 개인정보보호법상 문제가 될 수 있는 부분이 무엇인지 궁금합니다.

3. 특히 촬영 목적, 촬영 범위, 영상 저장 여부, 보관 기간 등에 따라 법적으로 고려해야 할 사항이 달라지는지 궁금합니다.

4. CCTV 영상을 활용한 서비스를 실제 기관에 도입한다고 했을 때, 가장 먼저 검토해야 하는 개인정보보호 또는 법적 요건은 무엇인지 궁금합니다.

5. CCTV 영상을 서버에 저장하지 않고 실시간으로 분석한 뒤 필요한 결과값만 저장하는 방식이라면, 일반적으로 영상을 직접 저장하는 방식보다 개인정보보호 측면에서 부담을 줄일 수 있는지 궁금합니다.

6. CCTV 영상에서 사람의 얼굴을 식별하지 않고, 사람이나 물품의 존재·행동 여부 정도만 분석하는 경우에도 개인정보 관련 규제가 동일하게 적용되는지 궁금합니다.

7. 실제 공공기관이나 기업에서 CCTV 기반 서비스를 도입할 때, 개인정보보호 문제 때문에 기술적으로 기능을 제한하거나 변경했던 사례가 있는지 궁금합니다.

8. 공공기관에 CCTV 기반 서비스를 제안한다고 가정했을 때, 기관 담당자 입장에서 가장 우려하거나 먼저 확인할 부분이 무엇인지 궁금합니다

---------


# Grok CLI 리팩터링 프롬프트

현재 저장소의 코드를 먼저 충분히 분석한 뒤 아래 요구사항에 맞게 리팩터링해줘.

## 목표

현재 키오스크의 STORE/RETRIEVE 얼굴 인증이 다음 AWS 파이프라인을 거친다.

```text
Browser
→ FastAPI /api/capture-frame
→ Kinesis
→ imageprocessor Lambda
→ S3 frames/
→ DynamoDB EnrichedFrame
→ facecompare Lambda
→ Rekognition CompareFaces
```

이 구조를 **키오스크 얼굴 인증에 한해서 단순화**하고 싶다.

최종 목표는 다음과 같다.

```text
STORE
신분증 Bytes + FRAME-A Bytes
→ FastAPI
→ Rekognition CompareFaces 직접 호출
→ 인증 성공
→ transaction 생성/완료 시 FRAME-A만
   S3 locker-references/<transaction_id>/reference.jpg 저장

RETRIEVE
S3 reference.jpg + 현재 FRAME-B Bytes
→ FastAPI
→ Rekognition CompareFaces 직접 호출
→ 성공하면 RETRIEVING/RETRIEVED
→ reference.jpg 삭제
```

중요:

* **키오스크 STORE/RETRIEVE에서는 Kinesis를 사용하지 않는다.**
* **키오스크 STORE/RETRIEVE에서는 DynamoDB EnrichedFrame을 사용하지 않는다.**
* **키오스크 얼굴 이미지를 `frames/...jpg`에 저장하지 않는다.**
* 기존 운영자 UI/CCTV/영상 분석용 Kinesis → Lambda → S3 → DynamoDB 파이프라인은 삭제하지 말고 유지한다.

---

## 1. STORE 변경

현재 STORE 흐름:

```text
ID image
+
FRAME-A

→ /api/capture-frame
→ Kinesis
→ S3 frames/<FRAME-A>.jpg
→ DynamoDB
→ facecompare Lambda
→ ID vs FRAME-A
→ 인증 성공
→ frames/<FRAME-A>.jpg를
   locker-references/<transaction_id>/reference.jpg로 CopyObject
```

이를 아래 구조로 변경한다.

```text
Browser memory

ID image Bytes
+
FRAME-A Bytes
        │
        ▼
FastAPI
        │
        ▼
AWS Rekognition CompareFaces

SourceImage = ID Bytes
TargetImage = FRAME-A Bytes

        │
Similarity >= configured threshold
        │
        ▼
인증 성공
```

STORE 얼굴 인증 과정에서는 다음을 하지 않는다.

```text
Kinesis PutRecord X
DynamoDB GetItem/PutItem X
S3 frames/ 저장 X
facecompare Lambda invoke X
```

FastAPI backend에서 boto3 Rekognition client를 이용하여 `compare_faces()`를 직접 호출한다.

기존 `FACE_SIMILARITY_THRESHOLD` 설정값을 그대로 사용한다.

---

## 2. STORE 인증 성공 후 FRAME-A 처리

중요한 요구사항:

얼굴 인증이 성공했다고 FRAME-A를 바로 S3에 저장하지 말 것.

인증 성공 당시에는 아직 locker reserve 전이라 `transaction_id`가 없을 수 있다.

따라서 다음 방식으로 구현한다.

### 브라우저

인증에 성공한 `FRAME-A` JPEG/Base64는 **브라우저 메모리에 계속 보관**한다.

localStorage, IndexedDB 등에 저장하지 않는다.

페이지 메모리에만 존재해야 한다.

### 서버

얼굴 이미지 Bytes 자체를 session cookie 등에 저장하지 않는다.

인증 성공 시 서버에는 최소한 다음 정보만 저장한다.

```text
verified_store_at
verified_store_face_hash
```

`verified_store_face_hash`는 FRAME-A JPEG bytes에 대한 SHA-256 등 안전한 digest를 사용한다.

기존 `verified_store_frame_id`는 제거하거나 더 이상 키오스크 STORE 인증에 사용하지 않도록 변경한다.

---

## 3. STORE complete 변경

locker reserve 이후 transaction_id가 생성되고 결제가 성공하면:

```text
POST /api/kiosk/store/complete
```

요청에 다음을 전달하도록 frontend/backend를 변경한다.

```json
{
  "transactionId": "...",
  "verifiedFaceImageBase64": "..."
}
```

서버에서는:

1. Base64 decode
2. 이미지 크기/형식 검사
3. SHA-256 계산
4. 세션의 `verified_store_face_hash`와 비교
5. 동일한 이미지인지 확인
6. 인증 유효시간도 검사
7. 모두 정상인 경우에만 S3 저장

S3 저장 위치:

```text
locker-references/<transaction_id>/reference.jpg
```

기존처럼:

```text
frames/.../<FRAME-A>.jpg
→ CopyObject
→ locker-references/...
```

하지 말고,

브라우저에서 전달받은 검증된 FRAME-A bytes를 바로:

```python
s3.put_object(
    Bucket=...,
    Key=f"locker-references/{transaction_id}/reference.jpg",
    Body=face_bytes,
    ContentType="image/jpeg",
)
```

형태로 저장한다.

가능하면 현재 프로젝트 S3 보안 설정 및 helper 구조를 재사용한다.

---

## 4. `reference_frame_id` 제거

키오스크 transaction에서는 더 이상 DynamoDB `EnrichedFrame`과 연결할 필요가 없다.

따라서:

```text
transactions.reference_frame_id
```

의존성을 제거한다.

기존 DB migration 및 오래된 DB와의 호환성을 고려해서 컬럼을 물리적으로 바로 DROP하는 것이 위험하면:

* 신규 코드에서 사용 중단
* nullable legacy column으로 남겨도 됨

둘 중 더 안전한 방식을 선택하고 이유를 설명해라.

---

## 5. `reference_face_s3_key` 처리

reference object 경로가 항상:

```text
locker-references/<transaction_id>/reference.jpg
```

로 결정되므로 `reference_face_s3_key`를 반드시 DB에 저장할 필요는 없다.

가능하면 다음 helper를 만든다.

```python
def build_reference_face_s3_key(transaction_id: str) -> str:
    return f"locker-references/{transaction_id}/reference.jpg"
```

STORE / RETRIEVE / DELETE에서 이 함수를 공통으로 사용한다.

`transactions.reference_face_s3_key` 컬럼 역시 기존 DB migration 호환성을 고려한다.

안전하게 제거하기 어렵다면 legacy nullable column으로 남기되 신규 코드에서는 source of truth로 사용하지 않는다.

---

## 6. RETRIEVE 변경

현재:

```text
FRAME-B
→ /api/capture-frame
→ Kinesis
→ imageprocessor
→ S3 frames/
→ DynamoDB
→ facecompare Lambda

reference S3
VS
FRAME-B S3
```

이를 제거한다.

새로운 RETRIEVE:

```text
8자리 retrieval code
        │
        ▼
transaction 조회
        │
        ▼
reference key 계산

locker-references/<transaction_id>/reference.jpg

        │
        │       Browser
        │       FRAME-B Bytes
        │           │
        ▼           ▼
AWS Rekognition CompareFaces

SourceImage:
{
    "S3Object": {
        "Bucket": reference_bucket,
        "Name": reference_key
    }
}

TargetImage:
{
    "Bytes": frame_b_bytes
}
```

FRAME-B는:

* S3에 저장하지 않는다.
* Kinesis에 보내지 않는다.
* DynamoDB에 저장하지 않는다.
* Lambda facecompare를 거치지 않는다.
* 비교 요청이 끝나면 서버 메모리에서도 더 이상 유지하지 않는다.

---

## 7. RETRIEVE API 수정

가능하면:

```text
POST /api/kiosk/retrieve/start
```

를 다음 형태로 변경한다.

기존:

```json
{
  "retrievalCode": "12345678",
  "targetFrameId": "uuid"
}
```

변경:

```json
{
  "retrievalCode": "12345678",
  "faceImageBase64": "..."
}
```

`targetFrameId`는 키오스크 RETRIEVE 경로에서 제거한다.

다음 기존 로직은 유지한다.

* retrieval code validation
* STORED + OCCUPIED 검증
* rate limiter
* transaction state machine
* RETRIEVAL_UNAVAILABLE 처리
* 얼굴 불일치 시 STORED 유지
* 얼굴 일치 시 STORED → RETRIEVING
* complete 시 RETRIEVING → RETRIEVED
* locker OCCUPIED → AVAILABLE

---

## 8. Reference 이미지 삭제

RETRIEVE 성공 후:

```text
RETRIEVING
→ RETRIEVED
```

전이가 완료되면 기존 정책과 동일하게:

```text
locker-references/<transaction_id>/reference.jpg
```

를 삭제한다.

기존:

```text
KIOSK_REFERENCE_DELETE_ON_RETRIEVE
```

설정이 있다면 최대한 유지한다.

삭제 실패 시에도 transaction이 이미 RETRIEVED라면 기존 정책처럼 transaction을 rollback하지 않는다.

---

## 9. 최종 개인정보 저장 구조

리팩터링 후 키오스크에서 이미지 lifecycle은 반드시 다음과 같아야 한다.

### STORE

```text
ID image
→ Browser memory
→ FastAPI memory
→ Rekognition
→ 저장하지 않음

FRAME-A
→ Browser memory
→ FastAPI memory
→ Rekognition
→ 인증 성공
→ Browser memory에 잠시 유지
→ store complete
→ S3 reference.jpg 1개만 저장
```

### RETRIEVE

```text
reference.jpg
→ S3에서 Rekognition Source로 사용

FRAME-B
→ Browser memory
→ FastAPI memory
→ Rekognition Target Bytes
→ 저장하지 않음

인증 성공
→ RETRIEVED
→ reference.jpg 삭제
```

최종적으로 한 active transaction에서 영구/준영구적으로 저장되는 얼굴 이미지는 최대:

```text
S3
locker-references/<transaction_id>/reference.jpg

1장
```

이어야 한다.

RETRIEVED 완료 후에는:

```text
0장
```

이어야 한다.

---

# 10. 기존 운영자 UI / 카메라 프레임 분석 파이프라인 유지

현재 프로젝트에는 키오스크 외에 별도의 운영자용 카메라 프레임 처리 기능이 존재한다.

현재 코드 기준:

```text
web-ui/index.html
web-ui/src/app.js
```

가 운영자 UI이며 다음 기능을 사용한다.

```text
웹캠 연속 캡처
        ↓
POST /api/capture-frame
        ↓
FastAPI
        ↓
Kinesis FrameStream
        ↓
imageprocessor Lambda
        ├── S3 frames/
        └── DynamoDB EnrichedFrame
```

또한 운영자 UI에는 다음 기존 기능이 존재한다.

```text
GET /enrichedframe
→ framefetcher Lambda
→ DynamoDB 조회
→ S3 presigned URL
→ 프레임 뷰어

POST /face-compare
→ facecompare Lambda
→ 최신 프레임 기반 Rekognition CompareFaces

DetectLabels ON/OFF
→ imageprocessor Lambda configuration 변경

KPI Dashboard
→ CloudWatch / DynamoDB 기반 파이프라인 KPI 조회
```

이번 리팩터링의 목표는 위 운영자 기능이나 데이터 파이프라인을 삭제하는 것이 아니다.

변경 대상은 오직:

```text
web-ui/kiosk.html
web-ui/src/kiosk.js
```

를 중심으로 하는 **키오스크 STORE/RETRIEVE 얼굴 인증 경로**이다.

변경 후 반드시 다음처럼 분리되어야 한다.

```text
=================================================
KIOSK FACE AUTHENTICATION
=================================================

STORE:
ID Bytes + FRAME-A Bytes
→ FastAPI
→ Rekognition CompareFaces

RETRIEVE:
S3 reference.jpg + FRAME-B Bytes
→ FastAPI
→ Rekognition CompareFaces

Kiosk에서 사용하지 않음:
- /api/capture-frame
- Kinesis
- DynamoDB EnrichedFrame
- S3 frames/
- captureId
- targetFrameId
- facecompare Lambda


=================================================
OPERATOR CAMERA FRAME PIPELINE
=================================================

운영자 브라우저
→ /api/capture-frame
→ FastAPI
→ Kinesis
→ imageprocessor
→ S3 frames/
→ DynamoDB EnrichedFrame

기존대로 유지한다.
```

따라서 다음 리소스와 코드는 삭제하지 않는다.

```text
/api/capture-frame

Kinesis FrameStream
imageprocessor Lambda
framefetcher Lambda
facecompare Lambda

S3 frames/
DynamoDB EnrichedFrame

web-ui/index.html
web-ui/src/app.js

/api/detect-labels
/enrichedframe
/face-compare

KPI dashboard
```

중요:

`/api/capture-frame`은 키오스크에서는 더 이상 호출하지 않지만 운영자 UI가 사용하므로 backend endpoint 자체를 삭제하지 않는다.

`facecompare` Lambda 역시 키오스크에서는 더 이상 호출하지 않지만 운영자 `/face-compare` 경로를 위해 유지한다.

`Kinesis → imageprocessor → S3/DynamoDB` 데이터 파이프라인 역시 운영자 카메라 프레임 처리 기능을 위해 유지한다.

이번 작업은:

```text
기존 AWS 데이터 파이프라인 삭제
```

가 아니라:

```text
키오스크 얼굴 인증을 기존 프레임 분석 파이프라인으로부터 분리
```

하는 리팩터링이다.


---

## 11. 수정 예상 파일

먼저 실제 repository를 확인한 후 결정하되 최소 다음 파일을 검토해라.

```text
web-ui/backend/app.py
web-ui/backend/reference_face.py
web-ui/backend/kiosk_store.py
web-ui/backend/config.py

web-ui/src/kiosk.js
web-ui/kiosk.html

tests/test_app_kiosk.py
tests/test_kiosk_store.py
```

필요하면 새로운 helper/module을 생성해도 된다.

예:

```text
web-ui/backend/kiosk_face.py
```

여기에:

```python
compare_id_to_face_bytes()
compare_reference_to_face_bytes()
decode_face_image()
hash_face_image()
build_reference_face_s3_key()
save_reference_face()
delete_reference_face()
```

등을 분리하는 것도 고려한다.

기존 `reference_face.py`를 적절히 리팩터링하는 것이 더 자연스럽다면 새 파일을 만들 필요는 없다.

---

## 12. Rekognition 에러 처리

기존 키오스크 frontend가 사용하는 reason/error contract를 최대한 유지한다.

예:

```text
SIMILARITY_ABOVE_THRESHOLD
SIMILARITY_BELOW_THRESHOLD
NO_FACE_IN_SOURCE_OR_TARGET
NO_FACE_IN_REFERENCE
NO_FACE_IN_TARGET
REFERENCE_FACE_MISSING
INVALID_IMAGE
IMAGE_TOO_LARGE
UNSUPPORTED_IMAGE_FORMAT
ACCESS_DENIED
THROTTLED
AWS_API_ERROR
SERVICE_ERROR
```

기존에 Kinesis/DynamoDB 비동기 처리 때문에 필요했던:

```text
TARGET_FRAME_NOT_READY
TARGET_FRAME_TOO_OLD
FRAME_METADATA_INVALID
FRAME_TIMEOUT
```

은 새 키오스크 direct-bytes 경로에서는 발생할 필요가 없다.

frontend의 동일-frame polling 로직 역시 STORE/RETRIEVE 키오스크 경로에서는 제거한다.

단, 운영자/CCTV 경로에 필요한 코드까지 제거하면 안 된다.

---

## 13. 보안 요구사항

다음을 지켜라.

* 신분증 bytes를 DB/S3에 저장하지 않는다.
* FRAME-B를 DB/S3에 저장하지 않는다.
* FRAME-A는 얼굴 인증 성공 + transaction 생성 이후 reference.jpg로 1회만 저장한다.
* image base64/raw bytes를 로그에 남기지 않는다.
* session cookie에 image bytes/base64를 저장하지 않는다.
* session에는 hash와 verification timestamp 같은 작은 metadata만 저장한다.
* 사용자에게 S3 key를 응답하지 않는다.
* 기존 인증/세션 보호를 유지한다.
* 이미지 최대 크기 validation을 유지한다.
* JPEG/PNG 검증을 유지한다.
* 가능하면 boto3 Rekognition client retry/config 패턴을 기존 AWS client와 일관되게 유지한다.

---

## 14. 테스트

기존 테스트를 단순 수정하는 수준에서 끝내지 말고 새로운 구조를 검증하는 테스트를 추가해라.

최소 다음 테스트가 필요하다.

### STORE

1. ID vs FRAME-A 성공 시 Rekognition Bytes-to-Bytes 호출 확인
2. 얼굴 불일치 시 reference S3가 생성되지 않음
3. 얼굴 성공 시에도 face-verify 단계에서는 S3가 생성되지 않음
4. store complete에서 전달한 FRAME-A hash가 인증 당시 hash와 다르면 거부
5. 정상 complete 시 reference.jpg가 정확히 1개 생성
6. Kinesis PutRecord가 호출되지 않음
7. DynamoDB GetItem이 호출되지 않음
8. facecompare Lambda invoke가 호출되지 않음

### RETRIEVE

1. Source=S3 reference / Target=Bytes로 CompareFaces 호출
2. FRAME-B S3 저장이 발생하지 않음
3. Kinesis 호출이 발생하지 않음
4. DynamoDB 호출이 발생하지 않음
5. mismatch면 transaction이 STORED 유지
6. match면 RETRIEVING
7. complete 후 RETRIEVED + reference 삭제
8. reference가 없으면 REFERENCE_FACE_MISSING

### 회귀 테스트

운영자 `/api/capture-frame`:

```text
FastAPI → Kinesis
```

기능은 기존대로 동작해야 한다.

---

## 15. 작업 방식

다음 순서로 진행해라.

1. 현재 repository 구조와 실제 호출 흐름을 먼저 분석
2. 현재 STORE/RETRIEVE sequence를 설명
3. 변경해야 할 파일과 이유를 정리
4. 구현
5. 테스트 수정/추가
6. 관련 테스트 실행
7. 실패하면 원인 수정 후 다시 실행
8. 최종 변경 내용을 요약

불필요한 대규모 rewrite는 하지 말고 최소 변경으로 구현한다.

현재 동작하는 transaction state machine, locker state machine, 로그인/세션, payment mock, locker mock은 깨뜨리지 않는다.

---

## 16. 작업 완료 후 반드시 보고할 내용

최종 답변에서 다음을 보여줘.

### A. 변경 전

```text
Kiosk
→ capture-frame
→ Kinesis
→ Lambda
→ S3 frames
→ DynamoDB
→ facecompare Lambda
→ Rekognition
```

### B. 변경 후

```text
STORE
ID Bytes + FRAME-A Bytes
→ FastAPI
→ Rekognition
→ verified
→ S3 reference.jpg 1개

RETRIEVE
S3 reference.jpg + FRAME-B Bytes
→ FastAPI
→ Rekognition
→ RETRIEVED
→ reference.jpg delete
```

### C. 삭제된 키오스크 의존성

```text
Kinesis
DynamoDB EnrichedFrame
S3 frames/
targetFrameId
captureId polling
facecompare Lambda
```

단, 이것들은 **키오스크 경로에서만 제거**되어야 하고 운영자/CCTV 파이프라인에서는 유지되어야 한다.

### D. 데이터 저장 결과

```text
신분증     저장 X
FRAME-A    reference.jpg 1개만 저장
FRAME-B    저장 X
RETRIEVED  reference 삭제 → 얼굴 이미지 0개
```

### E. 변경 파일 목록

각 파일별로:

```text
파일명
- 무엇을 변경했는지
- 왜 변경했는지
```

### F. 테스트 결과

실제로 실행한 테스트 명령과 pass/fail 개수를 보고한다.
